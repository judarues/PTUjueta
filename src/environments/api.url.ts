const local: string = 'https://localhost:44316/api'
const url: string = local;

export const api = {
  clientes: `${url}/Personas`,
  ventas: `${url}/ventas`,
  articulos: `${url}/Articulos`,
  encabezado: `${url}/encabezado`,

};
